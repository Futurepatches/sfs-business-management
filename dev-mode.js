/* SFS DEVELOPMENT MODE — DISABLED.
   This used to auto-inject a fake admin session so login could be skipped
   while the ERP was being built. Real login is now required, so this file
   intentionally does nothing. Left in place only so index.html doesn't
   need to be touched if a future step still references it. */
(function(){
  'use strict';
  // no-op — login bypass removed
})();
